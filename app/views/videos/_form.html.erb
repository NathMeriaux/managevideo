<%= simple_form_for(@video) do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :category, collection: Video::CATEGORIES, prompt: "Choisir le type de vidéo" %>
  <%= f.input :type %>
  <%= f.input :url %>

<% end %>

